{
  "name": "indiwari-cake-complete",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace indiwari-cake-backend\" \"npm run dev --workspace frontend\"",
    "start": "npm run start --workspace indiwari-cake-backend",
    "build": "npm run build --workspace frontend",
    "check": "npm run build --workspace frontend && node --check backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
